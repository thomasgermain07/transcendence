export function addFriend(user: any) {
  console.log(`add ${user.name} to friends`) // TODO : Make api request
}

export function removeFriend(user: any) {
  console.log(`remove ${user.name} from friends`) // TODO : Make api request
}
